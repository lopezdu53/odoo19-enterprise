<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ================================================================ -->
    <!-- Plantillas 1 (Cronograma) y 2 (Operario): comparten head/estilos -->
    <!-- ================================================================ -->
    <template id="kiosk_page" name="TV Calendar Kiosk">
        <html t-att-lang="'es'">
            <head>
                <meta charset="utf-8"/>
                <meta name="viewport" content="width=device-width, initial-scale=1"/>
                <t t-if="refresh_interval and refresh_interval &gt; 0">
                    <meta http-equiv="refresh" t-att-content="refresh_interval"/>
                </t>
                <title t-esc="board.name"/>
                <style>
                    :root {
                        --bg: #eef1f5; --panel: #ffffff; --ink: #1a2233;
                        --muted: #7a8699; --grid-line: #d5dbe4; --today: #0b57d0;
                        --header-bg: #0b1f3a; --header-ink: #ffffff;
                        --out-month: #f4f6f9; --weekend: #f7f9fc; --accent: #0b57d0;
                    }
                    body.theme-dark {
                        --bg: #0c1220; --panel: #131c2e; --ink: #eef2f8;
                        --muted: #93a1b8; --grid-line: #26324a; --today: #4f8cff;
                        --header-bg: #050b16; --header-ink: #ffffff;
                        --out-month: #0e1626; --weekend: #101a2c; --accent: #4f8cff;
                    }
                    * { box-sizing: border-box; margin: 0; padding: 0; }
                    html, body { height: 100%; }
                    body {
                        background: var(--bg); color: var(--ink);
                        font-family: "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
                        height: 100vh; display: flex; flex-direction: column;
                        overflow: hidden; -webkit-font-smoothing: antialiased;
                    }
                    header.tvc-header {
                        background: var(--header-bg); color: var(--header-ink);
                        display: flex; align-items: center; justify-content: space-between;
                        padding: 1.2vh 2vw; flex: 0 0 auto; gap: 1vw;
                    }
                    .tvc-title { display: flex; align-items: baseline; gap: 1.2vw; flex-wrap: wrap; }
                    .tvc-month { font-size: clamp(22px, 3.2vw, 54px); font-weight: 800; }
                    .tvc-year { font-size: clamp(16px, 1.8vw, 30px); font-weight: 400; opacity: .7; }
                    .tvc-board { font-size: clamp(13px, 1.3vw, 22px); opacity: .8; }
                    .tvc-right { display: flex; gap: 1.4vw; align-items: center; flex-wrap: wrap; }
                    .tvc-legend { display: flex; gap: 1.2vw; align-items: center; flex-wrap: wrap; }
                    .tvc-legend .item { display: flex; align-items: center; gap: .5vw; font-size: clamp(12px, 1.1vw, 20px); }
                    .tvc-legend .dot { width: 1.1vw; height: 1.1vw; min-width: 12px; min-height: 12px; border-radius: 50%; }
                    .tvc-legend .cnt { font-weight: 700; }
                    .tvc-clock { font-size: clamp(20px, 2.2vw, 44px); font-weight: 800; opacity: 1; white-space: nowrap; font-variant-numeric: tabular-nums; }
                    .op-chip {
                        background: var(--accent); color: #fff; font-weight: 800;
                        padding: .5vh 1.1vw; border-radius: 999px;
                        font-size: clamp(15px, 1.6vw, 30px); white-space: nowrap;
                    }

                    /* -------- Cronograma -------- */
                    .tvc-weekdays { display: grid; gap: 2px; padding: 4px 4px 0 4px; flex: 0 0 auto; }
                    .tvc-weekdays .wd {
                        text-align: center; font-size: clamp(12px, 1.3vw, 24px);
                        font-weight: 700; text-transform: uppercase; letter-spacing: .5px;
                        color: var(--muted); padding: .6vh 0;
                    }
                    .tvc-grid { display: grid; gap: 2px; padding: 0 4px 4px 4px; flex: 1 1 auto; min-height: 0; }
                    .tvc-cell {
                        background: var(--panel); border: 1px solid var(--grid-line);
                        border-radius: 8px; padding: .5vh .5vw; display: flex;
                        flex-direction: column; min-height: 0; overflow: hidden;
                    }
                    .tvc-cell.out { background: var(--out-month); }
                    .tvc-cell.out .daynum { opacity: .35; }
                    .tvc-cell.weekend { background: var(--weekend); }
                    .tvc-cell.today { border-color: var(--today); box-shadow: inset 0 0 0 3px var(--today); }
                    .tvc-cell .daynum {
                        font-size: clamp(15px, 1.6vw, 30px); font-weight: 700; line-height: 1;
                        margin-bottom: .4vh; display: flex; justify-content: space-between; align-items: baseline;
                    }
                    .tvc-cell .daynum .wd { font-size: clamp(11px, 1vw, 20px); font-weight: 700; color: var(--muted); text-transform: uppercase; }
                    .tvc-cell.today .daynum { color: var(--today); }
                    .tvc-cell.today .daynum .wd { color: var(--today); }
                    .tvc-cell .daynum .badge { background: var(--today); color: #fff; border-radius: 999px; padding: .2vh .8vw; }
                    .tvc-tasks { display: flex; flex-direction: column; gap: .5vh; overflow: hidden; }
                    .tvc-task {
                        color: #fff; border-radius: 6px; padding: .5vh .7vw;
                        line-height: 1.2; overflow: hidden;
                    }
                    .tvc-task .t-time { font-size: clamp(9px, .85vw, 16px); font-weight: 600; opacity: .85; white-space: nowrap; display: block; }
                    .tvc-task .t-name {
                        font-size: clamp(12px, 1.2vw, 24px); font-weight: 800; margin-top: .1vh;
                        overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
                    }
                    .tvc-task .t-name .check { margin-right: .3vw; }
                    .tvc-task .t-boards { font-size: clamp(9px, .85vw, 16px); font-weight: 700; opacity: .95; margin-top: .15vh; }
                    .tvc-task .t-boards .bd { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
                    .tvc-task .t-desc {
                        font-size: clamp(10px, .9vw, 17px); font-weight: 400; opacity: .95; margin-top: .2vh;
                        white-space: pre-line; overflow: hidden;
                        display: -webkit-box; -webkit-line-clamp: 10; -webkit-box-orient: vertical;
                    }
                    .tvc-task.done { opacity: .55; text-decoration: line-through; }
                    .tvc-task .check { font-weight: 800; }
                    .tvc-overflow { font-size: clamp(11px, 1vw, 18px); font-weight: 700; color: var(--muted); padding-left: .3vw; }

                    /* Dia actual (extendido): tarea dividida 70% / 30% */
                    .tvc-task.tvc-split { display: flex; flex-direction: row; gap: .7vw; align-items: stretch; }
                    .tvc-split .ts-main { flex: 0 0 70%; max-width: 70%; min-width: 0; }
                    .tvc-split .ts-side { flex: 0 0 30%; max-width: 30%; min-width: 0;
                        border-left: 2px solid rgba(255,255,255,.35); padding-left: .6vw; }
                    .tvc-split .ts-side .t-time { display: block; margin-bottom: .3vh; }
                    .tvc-split .ts-side .t-boards { margin-top: 0; }
                    .tvc-split .ts-main .t-desc { -webkit-line-clamp: 10; }

                    /* Dia Extendido: fuente de tareas mas pequena (TV 55") */
                    .tvc-grid.ext .tvc-task { padding: .35vh .5vw; }
                    .tvc-grid.ext .tvc-task .t-time { font-size: clamp(8px, .7vw, 13px); }
                    .tvc-grid.ext .tvc-task .t-name { font-size: clamp(10px, .95vw, 18px); }
                    .tvc-grid.ext .tvc-task .t-boards { font-size: clamp(8px, .72vw, 13px); }
                    .tvc-grid.ext .tvc-task .t-desc { font-size: clamp(9px, .78vw, 14px); -webkit-line-clamp: 6; }
                    .tvc-grid.ext .tvc-cell .daynum { font-size: clamp(13px, 1.3vw, 24px); }
                    .tvc-grid.ext .tvc-cell .daynum .wd { font-size: clamp(10px, .95vw, 18px); }

                    /* -------- Operario -------- */
                    .op-main { flex: 1 1 auto; display: flex; min-height: 0; gap: 6px; padding: 6px; }

                    /* Panel grande (70%): TAREAS DE HOY */
                    .op-primary { flex: 7 1 0; min-width: 0; display: flex; flex-direction: column;
                        background: var(--panel); border: 1px solid var(--grid-line); border-radius: 10px; overflow: hidden; }
                    .op-caption { background: var(--accent); color: #fff; padding: .9vh 1.4vw; flex: 0 0 auto;
                        font-size: clamp(16px, 1.9vw, 34px); font-weight: 800; letter-spacing: .5px; }
                    .big-tasks { flex: 1 1 auto; display: flex; flex-direction: column; gap: 1vh; padding: 1.2vh 1.2vw; overflow: auto; }
                    .big-task { color: #fff; border-radius: 12px; padding: 1.4vh 1.4vw; display: flex; flex-direction: row; gap: 1.2vw; align-items: stretch; }
                    .big-task .bt-content { flex: 1 1 0; min-width: 0; display: flex; flex-direction: column; gap: .3vh; }
                    .big-task .bt-img { flex: 0 0 40%; display: flex; align-items: center; justify-content: center; }
                    .big-task .bt-img img { max-width: 100%; max-height: 34vh; border-radius: 8px; object-fit: contain; background: #fff; }
                    .big-task.done { opacity: .55; text-decoration: line-through; }
                    .big-task .bt-time { font-size: clamp(12px, 1.3vw, 26px); font-weight: 600; opacity: .85; white-space: nowrap; }
                    .big-task .bt-name { font-size: clamp(20px, 2.6vw, 52px); font-weight: 800; line-height: 1.1; margin-top: .2vh; }
                    .big-task .bt-desc { font-size: clamp(14px, 1.5vw, 30px); font-weight: 500; opacity: .95;
                        overflow: hidden; display: -webkit-box; -webkit-line-clamp: 10; -webkit-box-orient: vertical; }
                    .big-task .bt-desc p { margin: 0 0 .3em 0; }
                    .big-task .bt-desc p:last-child { margin-bottom: 0; }
                    .big-task .bt-desc img { max-width: 100%; max-height: 22vh; height: auto; border-radius: 6px; margin: .3em 0; }
                    .big-task .bt-desc a { color: #fff; text-decoration: underline; }
                    .big-empty { color: var(--muted); padding: 4vh 1.5vw; text-align: center; font-size: clamp(18px, 2vw, 38px); }

                    /* Panel estrecho (30%): seguridad + horario + avisos */
                    .op-secondary { flex: 3 1 0; min-width: 0; display: flex; flex-direction: column; gap: 6px; }
                    .op-epp { background: #d32f2f; color: #fff; border-radius: 10px; padding: 1vh 1vw; flex: 0 0 auto;
                        display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; gap: .4vh; }
                    .op-epp .icon { font-size: clamp(22px, 2.8vw, 44px); line-height: 1; }
                    .op-epp .txt { font-size: clamp(13px, 1.4vw, 26px); font-weight: 800; }
                    .op-ban2 { background: #212b3a; border: 2px solid #d32f2f; color: #fff; border-radius: 10px;
                        padding: .8vh 1vw; display: flex; align-items: center; justify-content: center; gap: .6vw; text-align: center; flex: 0 0 auto; }
                    .op-ban2 .icon { font-size: clamp(20px, 2.4vw, 38px); line-height: 1; }
                    .op-ban2 .txt { font-size: clamp(12px, 1.3vw, 24px); font-weight: 800; }
                    .op-sched { background: var(--panel); border: 1px solid var(--grid-line); border-radius: 10px;
                        flex: 1 1 0; display: flex; flex-direction: column; overflow: hidden; }
                    .op-sched .s-cap { background: var(--header-bg); color: #fff; padding: .6vh 1vw;
                        font-size: clamp(12px, 1.3vw, 22px); font-weight: 800; letter-spacing: .5px; flex: 0 0 auto; }
                    .op-sched .s-rows { flex: 1 1 auto; display: flex; flex-direction: column; min-height: 0; }
                    .s-row { flex: 1 1 0; display: flex; align-items: center; gap: .6vw; padding: 0 .8vw;
                        border-bottom: 1px solid var(--grid-line); min-height: 0; }
                    .s-row:last-child { border-bottom: 0; }
                    .s-time { flex: 0 0 34%; font-size: clamp(12px, 1.2vw, 22px); font-weight: 800;
                        color: var(--accent); font-variant-numeric: tabular-nums; }
                    .s-act { flex: 1 1 auto; font-size: clamp(11px, 1.1vw, 20px); font-weight: 700;
                        white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
                    .s-act.pill { color: #fff; border-radius: 6px; padding: .3vh .6vw; }
                    .op-notice { background: var(--panel); border: 1px solid var(--grid-line); border-radius: 10px;
                        flex: 0 0 auto; max-height: 26vh; display: flex; flex-direction: column; overflow: hidden; }
                    .op-notice .n-title { background: #f9a825; color: #1a2233; padding: .6vh 1vw;
                        font-size: clamp(12px, 1.3vw, 22px); font-weight: 800; letter-spacing: .5px; flex: 0 0 auto; }
                    .op-notice .n-body { padding: .8vh 1vw; font-size: clamp(12px, 1.2vw, 22px); font-weight: 600;
                        white-space: pre-wrap; overflow: auto; line-height: 1.3; }
                    .op-notice .n-empty { color: var(--muted); font-weight: 400; }

                    /* Mecanizado: horario compacto -> mas espacio para AVISOS */
                    .op-mecan .op-sched { flex: 0 0 auto; }
                    .op-mecan .op-sched .s-rows { flex: 0 0 auto; }
                    .op-mecan .s-row { flex: 0 0 auto; padding: .1vh .8vw; }
                    .op-mecan .s-time { font-size: clamp(10px, 1vw, 18px); }
                    .op-mecan .s-act { font-size: clamp(10px, .95vw, 17px); }
                    .op-mecan .s-act.pill { padding: .2vh .5vw; }
                    .op-mecan .op-notice { flex: 1 1 auto; max-height: none; }
                    .op-mecan .op-notice .n-body { font-size: clamp(14px, 1.5vw, 28px); }

                    /* Electricos: boton solicitar + modal */
                    .req-btn { margin-top: .5vh; background: rgba(255,255,255,.92); color: #111; border: 0;
                        border-radius: 8px; font-weight: 800; font-size: clamp(12px, 1.25vw, 24px);
                        padding: .6vh 1vw; cursor: pointer; align-self: flex-start; }
                    .req-modal { display: none; position: fixed; inset: 0; background: rgba(0,0,0,.6); z-index: 999;
                        align-items: center; justify-content: center; }
                    .req-modal .req-box { background: #fff; color: #111; border-radius: 14px; padding: 3vh 3vw;
                        width: min(80vw, 720px); display: flex; flex-direction: column; gap: 1.4vh; }
                    .req-modal .req-title { font-size: clamp(20px, 2.4vw, 40px); font-weight: 800; }
                    .req-modal label { font-size: clamp(14px, 1.5vw, 26px); font-weight: 700; }
                    .req-modal select { font-size: clamp(16px, 1.8vw, 30px); padding: 1.2vh 1vw; border-radius: 8px; border: 2px solid #bbb; }
                    .req-modal .req-actions { display: flex; gap: 1vw; justify-content: flex-end; margin-top: 1vh; }
                    .req-modal .req-actions button { font-size: clamp(16px, 1.8vw, 30px); font-weight: 800;
                        padding: 1.2vh 2vw; border-radius: 10px; border: 0; cursor: pointer; }
                    .req-modal #req-cancel { background: #e0e0e0; color: #111; }
                    .req-modal #req-send { background: #2e7d32; color: #fff; }
                    .req-modal #req-msg { font-size: clamp(14px, 1.5vw, 26px); font-weight: 700; color: #1565c0; min-height: 1.4em; }
                </style>
            </head>
            <body t-attf-class="theme-#{theme}">
                <t t-if="template_type in ('operator', 'mecanizado', 'electric')">
                    <!-- ============ OPERARIO / MECANIZADO / ELECTRICOS ============ -->
                    <header class="tvc-header">
                        <div class="tvc-title">
                            <span class="tvc-month" t-esc="month_name"/>
                            <span class="tvc-year" t-esc="year"/>
                            <span class="tvc-board" t-esc="board.name"/>
                        </div>
                        <div class="tvc-right">
                            <t t-if="operator_name">
                                <span class="op-chip">Operario: <t t-esc="operator_name"/></span>
                            </t>
                            <span class="tvc-clock" id="tvc-clock" t-esc="now_label"/>
                        </div>
                    </header>
                    <div t-attf-class="op-main#{' op-mecan' if is_mecan else ''}">
                        <div class="op-primary">
                            <div class="op-caption">TAREAS DE HOY — <t t-esc="today_label"/></div>
                            <div class="big-tasks">
                                <t t-foreach="today_tasks" t-as="task">
                                    <div t-attf-class="big-task#{' done' if task['done'] else ''}"
                                         t-attf-style="background:#{task['color']}">
                                        <div class="bt-content">
                                            <t t-if="task['created_ts']">
                                                <div class="bt-time" t-att-data-ts="task['created_ts']"/>
                                            </t>
                                            <div class="bt-name" t-esc="task['name']"/>
                                            <t t-if="task['description_text']">
                                                <div class="bt-desc" t-out="task['description_html']"/>
                                            </t>
                                            <t t-if="interactive">
                                                <button class="req-btn" t-att-data-task="task['id']">+ Solicitar componente</button>
                                            </t>
                                        </div>
                                        <t t-if="show_images and task['image']">
                                            <div class="bt-img"><img t-att-src="task['image']"/></div>
                                        </t>
                                    </div>
                                </t>
                                <t t-if="not today_tasks">
                                    <div class="big-empty">Sin tareas para hoy.</div>
                                </t>
                            </div>
                        </div>
                        <div class="op-secondary">
                            <div class="op-epp">
                                <div class="icon">&#9888;</div>
                                <div class="txt" t-esc="epp_message"/>
                            </div>
                            <t t-if="epp_message2">
                                <div class="op-ban2">
                                    <div class="icon">&#128683;</div>
                                    <div class="txt" t-esc="epp_message2"/>
                                </div>
                            </t>
                            <div class="op-sched">
                                <div class="s-cap">HORARIO DEL DIA — <t t-esc="today_label"/></div>
                                <div class="s-rows">
                                    <t t-foreach="slots" t-as="slot">
                                        <div class="s-row">
                                            <div class="s-time" t-esc="slot['time']"/>
                                            <t t-if="slot['activity']">
                                                <div class="s-act pill" t-attf-style="background:#{slot['color']}"
                                                     t-esc="slot['activity']"/>
                                            </t>
                                            <t t-else="">
                                                <div class="s-act" style="opacity:.35">—</div>
                                            </t>
                                        </div>
                                    </t>
                                    <t t-if="not slots">
                                        <div class="s-row"><div class="s-act" style="opacity:.5">
                                            Sin horario. Usa "Cargar horario estandar".
                                        </div></div>
                                    </t>
                                </div>
                            </div>
                            <div class="op-notice">
                                <div class="n-title">AVISOS</div>
                                <t t-if="notice_message">
                                    <div class="n-body" t-esc="notice_message"/>
                                </t>
                                <t t-else="">
                                    <div class="n-body n-empty">Sin avisos.</div>
                                </t>
                            </div>
                        </div>
                    </div>
                    <t t-if="interactive">
                        <span id="tvc-data" t-att-data-token="access_token" style="display:none"/>
                        <div id="req-modal" class="req-modal">
                            <div class="req-box">
                                <div class="req-title">Solicitar componente</div>
                                <label>Categoria</label>
                                <select id="req-cat"/>
                                <label>Producto</label>
                                <select id="req-prod"/>
                                <div id="req-msg"/>
                                <div class="req-actions">
                                    <button id="req-cancel">Cancelar</button>
                                    <button id="req-send">Pedir</button>
                                </div>
                            </div>
                        </div>
                        <script type="text/javascript" src="/tv_calendar/static/src/js/tv_kiosk_interactive.js"/>
                    </t>
                </t>
                <t t-else="">
                    <!-- ======================= CRONOGRAMA ======================= -->
                    <header class="tvc-header">
                        <div class="tvc-title">
                            <span class="tvc-month" t-esc="month_name"/>
                            <span class="tvc-year" t-esc="year"/>
                            <span class="tvc-board" t-esc="board.name"/>
                        </div>
                        <div class="tvc-right">
                            <div class="tvc-legend">
                                <t t-foreach="legend" t-as="lg">
                                    <div class="item">
                                        <span class="dot" t-attf-style="background:#{lg['color']}"/>
                                        <span t-esc="lg['label']"/>
                                        <span class="cnt" t-esc="lg['count']"/>
                                    </div>
                                </t>
                            </div>
                            <span class="tvc-clock" id="tvc-clock" t-esc="now_label"/>
                        </div>
                    </header>
                    <t t-if="show_weekdays_row">
                        <div class="tvc-weekdays" t-attf-style="grid-template-columns: repeat(#{num_columns}, 1fr);">
                            <t t-foreach="weekday_names" t-as="wd">
                                <div class="wd" t-esc="wd"/>
                            </t>
                        </div>
                    </t>
                    <div t-attf-class="tvc-grid#{' ext' if extended else ''}"
                         t-attf-style="grid-template-columns: repeat(#{num_columns}, 1fr); grid-template-rows: repeat(#{len(weeks)}, 1fr);">
                        <t t-foreach="weeks" t-as="week">
                            <t t-foreach="week" t-as="day">
                                <div t-attf-class="tvc-cell#{' out' if not day['in_month'] else ''}#{' today' if day['is_today'] else ''}#{' weekend' if day['is_weekend'] else ''}"
                                     t-attf-style="grid-column: span #{day['span']};">
                                    <div class="daynum">
                                        <t t-if="not show_weekdays_row">
                                            <span class="wd" t-esc="day['weekday_label']"/>
                                        </t>
                                        <t t-if="day['is_today']"><span class="badge" t-esc="day['day_num']"/></t>
                                        <t t-else=""><span t-esc="day['day_num']"/></t>
                                    </div>
                                    <div class="tvc-tasks">
                                        <t t-foreach="day['tasks']" t-as="task">
                                            <div t-attf-class="tvc-task#{' tvc-split' if day['split'] else ''}#{' done' if task['done'] else ''}"
                                                 t-attf-style="background:#{task['color']}">
                                                <t t-if="day['split']">
                                                    <!-- Dia actual: 70% titulo+desc / 30% fecha+tableros -->
                                                    <div class="ts-main">
                                                        <div class="t-name">
                                                            <t t-if="task['done']"><span class="check">&#10003;</span></t>
                                                            <t t-esc="task['name']"/>
                                                        </div>
                                                        <t t-if="task['description_text']">
                                                            <div class="t-desc" t-esc="task['description_text']"/>
                                                        </t>
                                                    </div>
                                                    <div class="ts-side">
                                                        <t t-if="task['created_ts']">
                                                            <span class="t-time" t-att-data-ts="task['created_ts']"/>
                                                        </t>
                                                        <t t-if="task['boards']">
                                                            <div class="t-boards">
                                                                <t t-foreach="task['boards']" t-as="bd">
                                                                    <div class="bd">&#9642; <t t-esc="bd"/></div>
                                                                </t>
                                                            </div>
                                                        </t>
                                                    </div>
                                                </t>
                                                <t t-else="">
                                                    <t t-if="task['created_ts']">
                                                        <span class="t-time" t-att-data-ts="task['created_ts']"/>
                                                    </t>
                                                    <div class="t-name">
                                                        <t t-if="task['done']"><span class="check">&#10003;</span></t>
                                                        <t t-esc="task['name']"/>
                                                    </div>
                                                    <t t-if="task['boards']">
                                                        <div class="t-boards">
                                                            <t t-foreach="task['boards']" t-as="bd">
                                                                <div class="bd">&#9642; <t t-esc="bd"/></div>
                                                            </t>
                                                        </div>
                                                    </t>
                                                    <t t-if="day['show_desc'] and task['description_text']">
                                                        <div class="t-desc" t-esc="task['description_text']"/>
                                                    </t>
                                                </t>
                                            </div>
                                        </t>
                                        <t t-if="day['overflow']">
                                            <div class="tvc-overflow">+<t t-esc="day['overflow']"/> mas</div>
                                        </t>
                                    </div>
                                </div>
                            </t>
                        </t>
                    </div>
                </t>

                <script type="text/javascript">
                    (function () {
                        function pad(n) { return (n &lt; 10 ? '0' : '') + n; }
                        function fmtTime(d) {
                            var h = d.getHours();
                            var ap = h &lt; 12 ? 'am' : 'pm';
                            var h12 = h % 12; if (h12 === 0) { h12 = 12; }
                            return pad(h12) + ':' + pad(d.getMinutes()) + ' ' + ap;
                        }
                        // Horas de creacion -> hora local del dispositivo (arregla la zona horaria)
                        var stamps = document.querySelectorAll('[data-ts]');
                        Array.prototype.forEach.call(stamps, function (el) {
                            var ts = parseInt(el.getAttribute('data-ts'), 10);
                            if (ts) {
                                var d = new Date(ts * 1000);
                                el.textContent = pad(d.getDate()) + '/' + pad(d.getMonth() + 1) + ' ' + fmtTime(d);
                            }
                        });
                        // Reloj en vivo
                        var clock = document.getElementById('tvc-clock');
                        if (clock) {
                            function tick() {
                                var d = new Date();
                                clock.textContent = pad(d.getDate()) + '/' + pad(d.getMonth() + 1) + '/' +
                                    d.getFullYear() + ' ' + fmtTime(d);
                            }
                            tick(); setInterval(tick, 15000);
                        }
                    })();
                </script>
            </body>
        </html>
    </template>

    <!-- ================================================================ -->
    <!-- Plantilla Proyectos por entregar (30 dias Lun-Sab + 2 minis)     -->
    <!-- ================================================================ -->
    <template id="kiosk_projects" name="TV Calendar Projects">
        <html t-att-lang="'es'">
            <head>
                <meta charset="utf-8"/>
                <meta name="viewport" content="width=device-width, initial-scale=1"/>
                <t t-if="refresh_interval and refresh_interval &gt; 0">
                    <meta http-equiv="refresh" t-att-content="refresh_interval"/>
                </t>
                <title t-esc="board.name"/>
                <style>
                    :root {
                        --bg: #eef1f5; --panel: #ffffff; --ink: #1a2233; --muted: #7a8699;
                        --grid-line: #d5dbe4; --today: #0b57d0; --header-bg: #0b1f3a;
                        --out: #f4f6f9; --holi: #c62828; --holi-bg: #fdecea;
                    }
                    body.theme-dark {
                        --bg: #0c1220; --panel: #131c2e; --ink: #eef2f8; --muted: #93a1b8;
                        --grid-line: #26324a; --today: #4f8cff; --header-bg: #050b16;
                        --out: #0e1626; --holi: #ff6b6b; --holi-bg: #3a1414;
                    }
                    * { box-sizing: border-box; margin: 0; padding: 0; }
                    html, body { height: 100%; }
                    body { background: var(--bg); color: var(--ink); height: 100vh; display: flex; flex-direction: column;
                        overflow: hidden; font-family: "Segoe UI", Roboto, Helvetica, Arial, sans-serif; -webkit-font-smoothing: antialiased; }
                    header.pj-header { background: var(--header-bg); color: #fff; display: flex; align-items: center;
                        justify-content: space-between; padding: 1vh 1.6vw; flex: 0 0 auto; gap: 1vw; }
                    .pj-title { display: flex; align-items: baseline; gap: 1vw; flex-wrap: wrap; }
                    .pj-title .t1 { font-size: clamp(20px, 2.4vw, 42px); font-weight: 800; }
                    .pj-title .t2 { font-size: clamp(14px, 1.6vw, 28px); font-weight: 700; opacity: .85; }
                    .pj-title .t3 { font-size: clamp(12px, 1.2vw, 20px); opacity: .7; }
                    .pj-clock { font-size: clamp(18px, 2vw, 40px); font-weight: 800; white-space: nowrap; font-variant-numeric: tabular-nums; }
                    .pj-legend { flex: 1 1 auto; display: flex; flex-direction: row; flex-wrap: nowrap; gap: 1vw;
                        align-items: center; justify-content: center; padding: 0 1vw; overflow: hidden; min-width: 0; }
                    .pj-legend .lg-group { display: flex; align-items: center; gap: .55vw; flex-wrap: nowrap; white-space: nowrap; }
                    .pj-legend .lg-title { font-size: clamp(7px, .72vw, 13px); font-weight: 800; opacity: .8; }
                    .pj-legend .lg-item { display: flex; align-items: center; gap: .2vw; font-size: clamp(7px, .72vw, 13px); font-weight: 600; white-space: nowrap; }
                    .pj-legend .lg-dot { width: .8vw; height: .8vw; min-width: 8px; min-height: 8px; border-radius: 3px; }

                    .pj-main { flex: 1 1 auto; display: flex; min-height: 0; gap: 6px; padding: 6px; }
                    .pj-left { flex: 78 1 0; min-width: 0; display: flex; flex-direction: column; }
                    .pj-weekdays { display: grid; grid-template-columns: repeat(6, 1fr); gap: 2px; flex: 0 0 auto; }
                    .pj-weekdays .wd { text-align: center; font-size: clamp(9px, .85vw, 16px); font-weight: 800;
                        text-transform: uppercase; letter-spacing: .5px; color: var(--muted); padding: .35vh 0; }
                    .pj-grid { display: grid; grid-template-columns: repeat(6, 1fr); gap: 2px; flex: 1 1 auto; min-height: 0; padding-top: 2px; }
                    .pj-cell { background: var(--panel); border: 1px solid var(--grid-line); border-radius: 7px;
                        padding: .3vh .3vw; display: flex; flex-direction: column; min-height: 0; overflow: hidden; }
                    .pj-cell.out { background: var(--out); opacity: .5; }
                    .pj-cell.today { border-color: var(--today); box-shadow: inset 0 0 0 3px var(--today); }
                    .pj-cell.holi { background: var(--holi-bg); }
                    .pj-cell .dn { display: flex; justify-content: space-between; align-items: baseline; line-height: 1; margin-bottom: .3vh; }
                    .pj-cell .dn .wd { font-size: clamp(7px, .68vw, 13px); font-weight: 700; color: var(--muted); text-transform: uppercase; }
                    .pj-cell .dn .num { font-size: clamp(11px, 1.1vw, 21px); font-weight: 800; }
                    .pj-cell.today .dn .num, .pj-cell.today .dn .wd { color: var(--today); }
                    .pj-festivo { color: #fff; background: var(--holi); border-radius: 4px; padding: .1vh .4vw; margin-bottom: .25vh;
                        font-size: clamp(6px, .58vw, 11px); font-weight: 800; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
                    .pj-projects { display: flex; flex-direction: column; gap: .3vh; overflow: hidden; }
                    .pj-card { color: #fff; border-radius: 5px; padding: .2vh .4vw; line-height: 1.08; overflow: hidden;
                        display: flex; flex-direction: row; gap: .4vw; align-items: stretch; }
                    .pj-card.done { opacity: .55; text-decoration: line-through; }
                    .pj-card .pc-main { flex: 0 0 60%; max-width: 60%; min-width: 0; }
                    .pj-card .pc-dates { flex: 0 0 40%; max-width: 40%; min-width: 0;
                        border-left: 2px solid rgba(255,255,255,.35); padding-left: .4vw; }
                    .pj-card .c-client { font-size: clamp(7px, .63vw, 12px); font-weight: 800;
                        white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
                    .pj-card .c-row { font-size: clamp(6px, .5vw, 9px); font-weight: 500; opacity: .95;
                        white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
                    .pj-card .c-prog { margin-bottom: .25vh; }
                    .pj-card .c-prog .c-progbar { background: rgba(255,255,255,.3); border-radius: 3px;
                        height: .7vh; min-height: 4px; overflow: hidden; }
                    .pj-card .c-prog .c-progfill { background: #fff; height: 100%; }
                    .pj-card .c-prog .c-progtxt { font-size: clamp(6px, .52vw, 10px); font-weight: 800;
                        margin-top: .1vh; white-space: nowrap; }
                    .pj-card .c-date { margin-bottom: .15vh; }
                    .pj-card .c-date .c-lbl { display: block; font-size: clamp(5px, .44vw, 8px); font-weight: 600;
                        opacity: .8; text-transform: uppercase; letter-spacing: .2px; line-height: 1.02; }
                    .pj-card .c-date .c-val { display: block; font-size: clamp(7px, .58vw, 11px); font-weight: 800;
                        line-height: 1.05; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
                    .pj-card .c-desc { font-size: clamp(6px, .55vw, 11px); font-weight: 600; opacity: .95; margin-top: .05vh;
                        white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
                    .pj-card .c-mac { font-size: clamp(6px, .52vw, 10px); font-weight: 700; margin-top: .1vh;
                        white-space: normal; overflow: hidden; display: -webkit-box; -webkit-line-clamp: 4; -webkit-box-orient: vertical; }
                    /* Tipos que solo muestran el cliente */
                    .pj-card.simple { flex-direction: column; align-items: flex-start; justify-content: center; gap: .2vh; }
                    .pj-card.simple .c-type { font-size: clamp(6px, .5vw, 9px); font-weight: 800; text-transform: uppercase;
                        letter-spacing: .2px; background: rgba(0,0,0,.22); border-radius: 3px; padding: .1vh .4vw; }
                    .pj-card.simple .c-client-big { font-size: clamp(8px, .8vw, 15px); font-weight: 800; line-height: 1.05;
                        white-space: normal; overflow: hidden; display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical; }
                    .pj-overflow { font-size: clamp(10px, .9vw, 16px); font-weight: 700; color: var(--muted); padding-left: .2vw; }

                    /* Sidebar mini calendarios */
                    .pj-side { flex: 22 1 0; min-width: 0; display: flex; flex-direction: column; gap: 6px; }
                    .pj-mini { flex: 1 1 0; background: var(--panel); border: 1px solid var(--grid-line); border-radius: 8px;
                        display: flex; flex-direction: column; overflow: hidden; }
                    .pj-mini .m-cap { background: var(--header-bg); color: #fff; padding: .5vh .8vw; text-align: center;
                        font-size: clamp(14px, 1.5vw, 26px); font-weight: 800; letter-spacing: .5px; flex: 0 0 auto; }
                    .pj-mini .m-cap .yr { font-weight: 400; opacity: .7; font-size: .8em; }
                    .pj-mini .m-wd { display: grid; grid-template-columns: repeat(5, 1fr); flex: 0 0 auto; }
                    .pj-mini .m-wd span { text-align: center; font-size: clamp(9px, .9vw, 16px); font-weight: 700; color: var(--muted); padding: .3vh 0; }
                    .pj-mini .m-body { flex: 1 1 auto; display: flex; flex-direction: column; min-height: 0; }
                    .pj-mini .m-week { display: grid; grid-template-columns: repeat(5, 1fr); flex: 1 1 0; }
                    .pj-mini .m-day { display: flex; align-items: center; justify-content: center; position: relative;
                        font-size: clamp(11px, 1.2vw, 22px); font-weight: 700; border-radius: 6px; margin: 1px; }
                    .pj-mini .m-day.o { color: var(--muted); opacity: .35; }
                    .pj-mini .m-day.h { background: var(--holi); color: #fff; }
                    .pj-mini .m-day.t { box-shadow: inset 0 0 0 3px var(--today); color: var(--today); }
                    .pj-mini .m-day .dot { position: absolute; bottom: 8%; width: 26%; height: 26%; min-width: 5px; min-height: 5px;
                        border-radius: 50%; background: var(--today); }
                    .pj-mini .m-day.h .dot { background: #fff; }
                </style>
            </head>
            <body t-attf-class="theme-#{theme}">
                <header class="pj-header">
                    <div class="pj-title">
                        <span class="t1" t-esc="title"/>
                        <span class="t2" t-esc="range_label"/>
                        <span class="t3" t-esc="board.name"/>
                    </div>
                    <div class="pj-legend">
                        <div class="lg-group">
                            <span class="lg-title">Tipo:</span>
                            <t t-foreach="type_legend" t-as="lg">
                                <span class="lg-item"><span class="lg-dot" t-attf-style="background:#{lg['color']}"/><t t-esc="lg['label']"/></span>
                            </t>
                        </div>
                        <div class="lg-group">
                            <span class="lg-title">Prioridad:</span>
                            <t t-foreach="priority_legend" t-as="lg">
                                <span class="lg-item"><span class="lg-dot" t-attf-style="background:#{lg['color']}"/><t t-esc="lg['label']"/></span>
                            </t>
                        </div>
                    </div>
                    <span class="pj-clock" id="tvc-clock" t-esc="now_label"/>
                </header>
                <div class="pj-main">
                    <div class="pj-left">
                        <div class="pj-weekdays">
                            <t t-foreach="weekday_names" t-as="wd"><div class="wd" t-esc="wd"/></t>
                        </div>
                        <div class="pj-grid" t-attf-style="grid-template-rows: repeat(#{len(weeks)}, 1fr);">
                            <t t-foreach="weeks" t-as="week">
                                <t t-foreach="week" t-as="day">
                                    <div t-attf-class="pj-cell#{' out' if not day['in_range'] else ''}#{' today' if day['is_today'] else ''}#{' holi' if day['holiday'] else ''}">
                                        <div class="dn">
                                            <span class="wd" t-esc="day['weekday_label']"/>
                                            <span class="num" t-esc="day['day_num']"/>
                                        </div>
                                        <t t-if="day['holiday']">
                                            <div class="pj-festivo" t-att-title="day['holiday']">FESTIVO: <t t-esc="day['holiday']"/></div>
                                        </t>
                                        <div class="pj-projects">
                                            <t t-foreach="day['projects']" t-as="p">
                                                <t t-if="p['delivery_type'] == 'project'">
                                                    <div t-attf-class="pj-card#{' done' if p['done'] else ''}" t-attf-style="background:#{p['color']}">
                                                        <div class="pc-main">
                                                            <div class="c-client" t-esc="p['client']"/>
                                                            <t t-if="p['description']"><div class="c-desc" t-esc="p['description']"/></t>
                                                            <t t-if="p['machines']"><div class="c-mac" t-esc="p['machines']"/></t>
                                                        </div>
                                                        <div class="pc-dates">
                                                            <div class="c-prog">
                                                                <div class="c-progbar"><div class="c-progfill" t-attf-style="width:#{p['progress']}%;"/></div>
                                                                <div class="c-progtxt"><t t-esc="p['progress']"/>% · <t t-esc="p['done_count']"/>/<t t-esc="p['total_count']"/></div>
                                                            </div>
                                                            <t t-if="p['order_ref']">
                                                                <div class="c-date"><span class="c-lbl">OC / Cot.</span><span class="c-val" t-esc="p['order_ref']"/></div>
                                                            </t>
                                                            <t t-if="p['order_date']">
                                                                <div class="c-date"><span class="c-lbl">Orden</span><span class="c-val" t-esc="p['order_date']"/></div>
                                                            </t>
                                                            <div class="c-date"><span class="c-lbl">Entrega</span><span class="c-val" t-esc="p['delivery_date']"/></div>
                                                        </div>
                                                    </div>
                                                </t>
                                                <t t-else="">
                                                    <div t-attf-class="pj-card simple#{' done' if p['done'] else ''}"
                                                         t-attf-style="background:#{p['color']}" t-att-title="p['type_label']">
                                                        <div class="c-client-big" t-esc="p['client']"/>
                                                    </div>
                                                </t>
                                            </t>
                                            <t t-if="day['overflow']">
                                                <div class="pj-overflow">+<t t-esc="day['overflow']"/> mas</div>
                                            </t>
                                        </div>
                                    </div>
                                </t>
                            </t>
                        </div>
                    </div>
                    <div class="pj-side">
                        <t t-foreach="minis" t-as="mini">
                            <div class="pj-mini">
                                <div class="m-cap"><t t-esc="mini['month_name']"/> <span class="yr" t-esc="mini['year']"/></div>
                                <div class="m-wd">
                                    <t t-foreach="mini['weekday_names']" t-as="w"><span t-esc="w"/></t>
                                </div>
                                <div class="m-body">
                                    <t t-foreach="mini['weeks']" t-as="mweek">
                                        <div class="m-week">
                                            <t t-foreach="mweek" t-as="md">
                                                <div t-attf-class="m-day#{' o' if not md['in_month'] else ''}#{' h' if (md['in_month'] and md['holiday']) else ''}#{' t' if md['is_today'] else ''}">
                                                    <t t-esc="md['day_num']"/>
                                                    <t t-if="md['in_month'] and md['delivery']"><span class="dot"/></t>
                                                </div>
                                            </t>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </t>
                    </div>
                </div>
                <script type="text/javascript">
                    (function () {
                        var el = document.getElementById('tvc-clock');
                        if (!el) { return; }
                        function pad(n) { return (n &lt; 10 ? '0' : '') + n; }
                        function tick() {
                            var d = new Date(); var h = d.getHours(); var ap = h &lt; 12 ? 'am' : 'pm';
                            var h12 = h % 12; if (h12 === 0) { h12 = 12; }
                            el.textContent = pad(d.getDate()) + '/' + pad(d.getMonth() + 1) + '/' + d.getFullYear() +
                                ' ' + pad(h12) + ':' + pad(d.getMinutes()) + ' ' + ap;
                        }
                        tick(); setInterval(tick, 15000);
                    })();
                </script>
            </body>
        </html>
    </template>

    <!-- ================================================================ -->
    <!-- Plantilla Pedidos de Componentes (interactiva)                   -->
    <!-- ================================================================ -->
    <template id="kiosk_component_orders" name="TV Component Orders">
        <html t-att-lang="'es'">
            <head>
                <meta charset="utf-8"/>
                <meta name="viewport" content="width=device-width, initial-scale=1"/>
                <t t-if="refresh_interval and refresh_interval &gt; 0">
                    <meta http-equiv="refresh" t-att-content="refresh_interval"/>
                </t>
                <title t-esc="board.name"/>
                <style>
                    :root { --bg:#eef1f5; --panel:#fff; --ink:#1a2233; --muted:#7a8699; --grid-line:#d5dbe4;
                        --header-bg:#0b1f3a; --accent:#0b57d0; }
                    body.theme-dark { --bg:#0c1220; --panel:#131c2e; --ink:#eef2f8; --muted:#93a1b8; --grid-line:#26324a;
                        --header-bg:#050b16; --accent:#4f8cff; }
                    * { box-sizing:border-box; margin:0; padding:0; }
                    html, body { height:100%; }
                    body { background:var(--bg); color:var(--ink); height:100vh; display:flex; flex-direction:column;
                        overflow:hidden; font-family:"Segoe UI",Roboto,Helvetica,Arial,sans-serif; -webkit-font-smoothing:antialiased; }
                    header.co-header { background:var(--header-bg); color:#fff; display:flex; align-items:center;
                        justify-content:space-between; padding:1.2vh 2vw; flex:0 0 auto; gap:1vw; }
                    .co-title { display:flex; align-items:baseline; gap:1.2vw; flex-wrap:wrap; }
                    .co-title .t1 { font-size:clamp(20px,2.4vw,44px); font-weight:800; }
                    .co-title .t2 { font-size:clamp(13px,1.3vw,22px); opacity:.8; }
                    .co-chip { background:var(--accent); color:#fff; font-weight:800; padding:.5vh 1.1vw; border-radius:999px;
                        font-size:clamp(14px,1.5vw,28px); white-space:nowrap; }
                    .co-clock { font-size:clamp(18px,2vw,40px); font-weight:800; white-space:nowrap; font-variant-numeric:tabular-nums; }

                    .co-main { flex:1 1 auto; display:flex; min-height:0; gap:6px; padding:6px; }
                    .co-primary { flex:7 1 0; min-width:0; display:flex; flex-direction:column; background:var(--panel);
                        border:1px solid var(--grid-line); border-radius:10px; overflow:hidden; }
                    .co-caption { background:var(--accent); color:#fff; padding:.9vh 1.4vw; flex:0 0 auto;
                        font-size:clamp(16px,1.9vw,34px); font-weight:800; letter-spacing:.5px; }
                    .co-list { flex:1 1 auto; display:flex; flex-direction:column; gap:1vh; padding:1.2vh 1.2vw; overflow:auto; }
                    .co-card { background:var(--bg); border:1px solid var(--grid-line); border-radius:12px; padding:1vh 1vw;
                        display:flex; flex-direction:row; gap:1vw; align-items:stretch; }
                    .co-img { flex:0 0 16%; display:flex; align-items:center; justify-content:center; }
                    .co-img img { max-width:100%; max-height:16vh; border-radius:8px; object-fit:contain; background:#fff; }
                    .co-info { flex:1 1 0; min-width:0; display:flex; flex-direction:column; gap:.2vh; }
                    .co-info .co-t { font-size:clamp(15px,1.7vw,32px); font-weight:800; }
                    .co-info .co-cat { font-size:clamp(12px,1.3vw,24px); font-weight:700; color:var(--accent); }
                    .co-info .co-desc { font-size:clamp(12px,1.25vw,22px); }
                    .co-info .co-val { font-size:clamp(14px,1.5vw,28px); font-weight:800; }
                    .co-act { flex:0 0 27%; display:flex; flex-direction:column; gap:.5vh; justify-content:center; }
                    .co-act .row { display:flex; align-items:center; gap:.6vw; }
                    .comp-btn { flex:1 1 auto; font-size:clamp(11px,1.2vw,22px); font-weight:800; padding:.8vh .8vw;
                        border:0; border-radius:8px; cursor:pointer; color:#fff; }
                    .comp-btn.b-req { background:#ef6c00; }
                    .comp-btn.b-del { background:#2e7d32; }
                    .comp-btn.done { opacity:.45; }
                    .comp-time { flex:0 0 34%; font-size:clamp(10px,1vw,18px); font-weight:700; min-height:1.1em; text-align:right; }
                    .co-empty { color:var(--muted); padding:4vh 1.5vw; text-align:center; font-size:clamp(18px,2vw,38px); }

                    .co-side { flex:3 1 0; min-width:0; display:flex; flex-direction:column; gap:6px; }
                    .co-epp { background:#d32f2f; color:#fff; border-radius:10px; padding:1vh 1vw; flex:0 0 auto; text-align:center;
                        font-size:clamp(14px,1.5vw,28px); font-weight:800; }
                    .co-sched { background:var(--panel); border:1px solid var(--grid-line); border-radius:10px; flex:1 1 0;
                        display:flex; flex-direction:column; overflow:hidden; }
                    .co-sched .s-cap { background:var(--header-bg); color:#fff; padding:.6vh 1vw; font-size:clamp(12px,1.3vw,22px); font-weight:800; }
                    .co-sched .s-rows { flex:1 1 auto; display:flex; flex-direction:column; min-height:0; }
                    .co-sched .s-row { flex:1 1 0; display:flex; align-items:center; gap:.6vw; padding:0 .8vw; border-bottom:1px solid var(--grid-line); }
                    .co-sched .s-row:last-child { border-bottom:0; }
                    .co-sched .s-time { flex:0 0 34%; font-size:clamp(11px,1.1vw,20px); font-weight:800; color:var(--accent); }
                    .co-sched .s-act { flex:1 1 auto; font-size:clamp(10px,1vw,18px); font-weight:700; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
                    .co-sched .s-act.pill { color:#fff; border-radius:6px; padding:.2vh .5vw; }
                    .co-notice { background:var(--panel); border:1px solid var(--grid-line); border-radius:10px; flex:0 0 auto;
                        max-height:26vh; display:flex; flex-direction:column; overflow:hidden; }
                    .co-notice .n-title { background:#f9a825; color:#1a2233; padding:.6vh 1vw; font-size:clamp(12px,1.3vw,22px); font-weight:800; }
                    .co-notice .n-body { padding:.8vh 1vw; font-size:clamp(12px,1.2vw,22px); font-weight:600; white-space:pre-wrap; overflow:auto; }
                </style>
            </head>
            <body t-attf-class="theme-#{theme}">
                <span id="tvc-data" t-att-data-token="access_token" style="display:none"/>
                <header class="co-header">
                    <div class="co-title">
                        <span class="t1">Pedidos de Componentes</span>
                        <span class="t2" t-esc="board.name"/>
                    </div>
                    <t t-if="operator_name"><span class="co-chip">Operario: <t t-esc="operator_name"/></span></t>
                    <span class="co-clock" id="tvc-clock" t-esc="now_label"/>
                </header>
                <div class="co-main">
                    <div class="co-primary">
                        <div class="co-caption">SOLICITUDES DE COMPONENTES</div>
                        <div class="co-list">
                            <t t-foreach="requests" t-as="r">
                                <div class="co-card">
                                    <t t-if="r['image']"><div class="co-img"><img t-att-src="r['image']"/></div></t>
                                    <div class="co-info">
                                        <div class="co-t" t-esc="r['name']"/>
                                        <t t-if="r['category']"><div class="co-cat" t-esc="r['category']"/></t>
                                        <t t-if="r['description']"><div class="co-desc" t-esc="r['description']"/></t>
                                        <div class="co-val">$<t t-esc="r['value']"/></div>
                                    </div>
                                    <div class="co-act">
                                        <div class="row">
                                            <button t-attf-class="comp-btn b-req#{' done' if r['state'] in ('requested','delivered') else ''}"
                                                    t-att-data-id="r['id']" data-state="requested">Solicitado al proveedor</button>
                                            <span class="comp-time" t-attf-id="ts-req-#{r['id']}" t-att-data-ts="r['requested_ts']"/>
                                        </div>
                                        <div class="row">
                                            <button t-attf-class="comp-btn b-del#{' done' if r['state'] == 'delivered' else ''}"
                                                    t-att-data-id="r['id']" data-state="delivered">Entregado al operario</button>
                                            <span class="comp-time" t-attf-id="ts-del-#{r['id']}" t-att-data-ts="r['delivered_ts']"/>
                                        </div>
                                    </div>
                                </div>
                            </t>
                            <t t-if="not requests">
                                <div class="co-empty">No hay solicitudes pendientes.</div>
                            </t>
                        </div>
                    </div>
                    <div class="co-side">
                        <div class="co-epp" t-esc="epp_message"/>
                        <div class="co-sched">
                            <div class="s-cap">HORARIO DEL DIA — <t t-esc="today_label"/></div>
                            <div class="s-rows">
                                <t t-foreach="slots" t-as="slot">
                                    <div class="s-row">
                                        <div class="s-time" t-esc="slot['time']"/>
                                        <t t-if="slot['activity']">
                                            <div class="s-act pill" t-attf-style="background:#{slot['color']}" t-esc="slot['activity']"/>
                                        </t>
                                        <t t-else=""><div class="s-act" style="opacity:.35">—</div></t>
                                    </div>
                                </t>
                            </div>
                        </div>
                        <div class="co-notice">
                            <div class="n-title">AVISOS</div>
                            <t t-if="notice_message"><div class="n-body" t-esc="notice_message"/></t>
                            <t t-else=""><div class="n-body" style="opacity:.6">Sin avisos.</div></t>
                        </div>
                    </div>
                </div>
                <script type="text/javascript">
                    (function () {
                        var el = document.getElementById('tvc-clock');
                        if (!el) { return; }
                        function pad(n) { return (n &lt; 10 ? '0' : '') + n; }
                        function tick() {
                            var d = new Date(); var h = d.getHours(); var ap = h &lt; 12 ? 'am' : 'pm';
                            var h12 = h % 12; if (h12 === 0) { h12 = 12; }
                            el.textContent = pad(d.getDate()) + '/' + pad(d.getMonth() + 1) + '/' + d.getFullYear() +
                                ' ' + pad(h12) + ':' + pad(d.getMinutes()) + ' ' + ap;
                        }
                        tick(); setInterval(tick, 15000);
                    })();
                </script>
                <script type="text/javascript" src="/tv_calendar/static/src/js/tv_kiosk_interactive.js"/>
            </body>
        </html>
    </template>

    <!-- ================================================================ -->
    <!-- Plantilla 3: Publicidad (playlist de YouTube a pantalla completa) -->
    <!-- ================================================================ -->
    <template id="kiosk_ad" name="TV Calendar Ad">
        <html t-att-lang="'es'">
            <head>
                <meta charset="utf-8"/>
                <meta name="viewport" content="width=device-width, initial-scale=1"/>
                <title t-esc="board.name"/>
                <style>
                    * { box-sizing: border-box; margin: 0; padding: 0; }
                    html, body { height: 100%; width: 100%; background: #000; overflow: hidden; }
                    iframe { border: 0; width: 100vw; height: 100vh; display: block; }
                    .ad-empty {
                        color: #fff; height: 100vh; display: flex; align-items: center;
                        justify-content: center; text-align: center; padding: 5vw;
                        font-family: "Segoe UI", Roboto, Arial, sans-serif;
                        font-size: clamp(18px, 2.4vw, 40px); line-height: 1.5;
                    }
                </style>
            </head>
            <body>
                <t t-if="embed_url">
                    <iframe t-att-src="embed_url"
                            allow="autoplay; encrypted-media; fullscreen"
                            allowfullscreen="allowfullscreen"/>
                </t>
                <t t-else="">
                    <div class="ad-empty">
                        <div>
                            <p>Configura la <b>URL de la playlist de YouTube</b> en el tablero.</p>
                            <p style="opacity:.6; font-size:.6em; margin-top:1em;">
                                Debe contener <code>list=...</code> (una lista de reproduccion, no un solo video).
                            </p>
                        </div>
                    </div>
                </t>
            </body>
        </html>
    </template>
</odoo>
